Хорошо, давайте сделаем **полный и подробный README.md**, который будет профессионально описывать и веб-версию, и десктопную версию (.exe), и ваш специфический кейс с глэмпингами. Я возьму всё полезное из обоих файлов, уберу локальные пути (чтобы это выглядело как публичный Open Source проект) и структурирую всё красиво.

Вот готовый код для файла `README.md`. Скопируйте его целиком.

```markdown
# 🔍 AI-Анализатор Глэмпингов (Competitor Monitor)

Полнофункциональное приложение (Web + Desktop) для анализа конкурентной среды в нише эко-туризма с поддержкой мультимодального анализа (текст и изображения).

![Python](https://img.shields.io/badge/Python-3.11+-blue.svg)
![FastAPI](https://img.shields.io/badge/FastAPI-Backend-green.svg)
![PyQt6](https://img.shields.io/badge/PyQt6-Desktop-blue.svg)
![OpenAI](https://img.shields.io/badge/AI-Gemini%20%2F%20GPT4-purple.svg)
![Selenium](https://img.shields.io/badge/Selenium-Parsing-yellow.svg)

## 📋 Описание кейса

В рамках проекта разработан инструмент для автоматического мониторинга сайтов глэмпингов ("Лес и Море", "Scala", "Китовый берег"). 
Приложение помогает маркетологу за секунды оценить визуальный стиль конкурентов, "инстаграмность" локаций и качество их офферов.

**Ключевые возможности:**
- **Анализ текста:** Выявляет уникальные услуги (фурако, баня, экскурсии), оценивает Tone of Voice и продающие качества текста.
- **Анализ изображений:** Vision-модель ставит баллы за "уют" (Coziness Score), "премиальность" и предлагает идеи для анимации сайта.
- **Авто-парсинг:** Selenium автоматически заходит на сайты конкурентов, делает скриншоты и собирает заголовки для анализа.
- **Мультиплатформенность:** Работает как веб-сервис в браузере или как нативное Windows-приложение (.exe).

---

## 🚀 Вариант 1: Запуск Веб-версии

Веб-версия запускает локальный сервер с удобным интерфейсом в браузере.

### 1. Установка
```bash
# Клонируйте репозиторий
git clone https://github.com/ВАШ_НИКНЕЙМ/pem08.git
cd pem08

# Создайте и активируйте окружение
python -m venv venv
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка (.env)
Создайте файл `.env` в корне проекта (используйте `env.example.txt` как шаблон):
```env
OPENROUTER_API_KEY=ваш_ключ_здесь
OPENAI_MODEL=google/gemini-2.5-flash-lite-preview-09-2025
OPENAI_VISION_MODEL=google/gemini-3-pro-image-preview
API_HOST=0.0.0.0
API_PORT=8000
```

### 3. Запуск
```bash
python run.py
```
Интерфейс доступен по адресу: **http://localhost:8000**  
Документация API: **http://localhost:8000/docs**

---

## 💻 Вариант 2: Десктопная версия (Windows)

Приложение имеет полноценный графический интерфейс на **PyQt6**. Вы можете запустить его из кода или собрать в `.exe` файл.

### Запуск из исходников
```bash
python launcher.py
```
*Откроется окно с вкладками "Анализ текста", "Анализ изображения" и "Парсинг".*

### Сборка в .exe файл
Вы можете собрать приложение в один исполняемый файл, чтобы передавать его коллегам (Python не потребуется).

```bash
# Сборка в папку (быстрый старт)
python build.py --onedir

# Сборка в один файл (дольше, но компактнее)
python build.py --onefile
```
Готовый файл появится в папке: `dist/CompetitorMonitor.exe`

---

## 🔧 Как это работает (Под капотом)

### 1. Backend (`backend/`)
- **FastAPI** отвечает за API.
- **OpenAI Service** отправляет промпты, специально настроенные под глэмпинги (оценка атмосферы, поиск уникальных услуг).
- **Parser Service** использует `Selenium` и `Chrome WebDriver` для эмуляции реального пользователя.

### 2. Frontend (`frontend/`)
- Простой HTML/JS интерфейс, который общается с бекендом.

### 3. Desktop App (`desktop_app/`)
- Нативное приложение на **PyQt6**, которое дублирует функционал веба, но работает как обычная программа Windows.

---

## 📁 Структура проекта

```
competitor-monitor/
├── backend/
│   ├── config.py            # Настройки и список URL конкурентов
│   ├── main.py              # Логика сервера
│   ├── services/
│       ├── openai_service.py    # Промпты для анализа (System Prompts)
│       ├── parser_service.py    # Логика Selenium парсера
├── desktop_app/             # Исходный код PyQt6 приложения
├── frontend/                # Файлы веб-интерфейса
├── data/                    # Скриншоты глэмпингов для тестов
├── build.py                 # Скрипт сборки EXE
├── launcher.py              # Точка входа для Desktop
├── run.py                   # Точка входа для Web
├── requirements.txt         # Список библиотек
└── README.md                # Документация
```

## ⚠️ Системные требования

1.  **Google Chrome**: Должен быть установлен для работы автоматического парсинга.
2.  **API Key**: Действующий ключ OpenRouter или OpenAI.
3.  **OS**: Windows 10/11 (для десктопной версии). Веб-версия работает везде.

## 📝 Лицензия

MIT License
```
